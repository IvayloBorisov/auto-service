
import {Logo, Navbar, Hero} from '../index';

const Header = () => {

    return (
        <header>
            <Logo />
            <Navbar />
            <Hero />
        </header>
    )
}

export default Header;